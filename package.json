{
  "name": "through2-split",
  "version": "1.0.1",
  "description": "repack the original stream based on a delimiter",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "stream",
    "split-stream",
    "through2-split"
  ],
  "author": "ybybzj",
  "license": "ISC",
  "dependencies": {
    "through2": "^2.0.1"
  }
}
